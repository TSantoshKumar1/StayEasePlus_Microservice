server:
  port: 8085
spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/TSantoshKumar1/config_microservice_server
          clone-on-start: true

